import styles from "../../styles/Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <span className={styles.span}>
        News Portal | Todos os direitos reservados
      </span>
    </footer>
  );
};

export default Footer;
